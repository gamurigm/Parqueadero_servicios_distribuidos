apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tickets-db
  namespace: app-parqueadero
spec:
  serviceName: tickets-db-headless
  replicas: 1
  selector:
    matchLabels:
      app: tickets-db
  template:
    metadata:
      labels:
        app: tickets-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "TicketsDB"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: tickets-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: tickets-password
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "admin_user", "-d", "TicketsDB"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          volumeMounts:
            - name: tickets-db-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: tickets-db-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: tickets-db
  namespace: app-parqueadero
spec:
  selector:
    app: tickets-db
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: tickets-db-headless
  namespace: app-parqueadero
spec:
  clusterIP: None
  selector:
    app: tickets-db
  ports:
    - port: 5432
      targetPort: 5432
