apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: trazabilidad-db
  namespace: app-parqueadero
spec:
  serviceName: trazabilidad-db-headless
  replicas: 1
  selector:
    matchLabels:
      app: trazabilidad-db
  template:
    metadata:
      labels:
        app: trazabilidad-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "TrazabilidadDB"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: trazabilidad-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: trazabilidad-password
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "admin_user", "-d", "TrazabilidadDB"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          volumeMounts:
            - name: trazabilidad-db-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: trazabilidad-db-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: trazabilidad-db
  namespace: app-parqueadero
spec:
  selector:
    app: trazabilidad-db
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: trazabilidad-db-headless
  namespace: app-parqueadero
spec:
  clusterIP: None
  selector:
    app: trazabilidad-db
  ports:
    - port: 5432
      targetPort: 5432
